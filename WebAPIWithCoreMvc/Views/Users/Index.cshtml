@model IEnumerable<Entities.Dtos.UserDtos.UserDetail.Dto>
@{
    ViewData["Title"] = "Index";
}

<h1>Kullanıcılar</h1>
<a class="btn btn_primary mb-3" asp-action="Add">Kullacı Ekle</a>
<table>
    <thead>
        <tr>
            <td>Id</td>
            <td>Kullanıcı Adı</td>
            <td>Adı</td>
            <td>Soyadı</td>
            <td>Cinsiyeti</td>
            <td>Doğum Tarihi</td>
            <td>Email</td>
            <td>Adres</td>
            <td>İşlemler</td>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.UserName</td>
                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>@item.Gender</td>
                <td>@item.DateOfBirth</td>
                <td>@item.Email</td>
                <td>@item.Addresa</td>
                <td>
                    <a asp-action="Update" asp-route-id="@item.Id">Düzenle</a>
                    <a asp-action="Delte" asp-route-id="@item.Id">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
